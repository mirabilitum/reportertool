# Reporting Module

`reporting` owns chart data adaptation, rendering, deterministic text generation, QA previews, report assembly, HTML output, and DOCX output.

It consumes intermediate tables and report rules; it must not re-read raw Excel data or redo data cleaning.
