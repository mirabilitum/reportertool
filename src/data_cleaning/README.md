# Data Cleaning Module

`data_cleaning` owns raw Excel input handling: file pairing, title parsing, workbook structure detection, row normalization, and data cleaning/merge preparation.

It may consume questionnaire mapping outputs from `questionnaire`, but it must not perform question merging itself.
