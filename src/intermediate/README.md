# Intermediate Module

`intermediate` owns stable analysis tables: `base_answer_fact`, `question_table`, data reconstruction rules, reconstructed indicator facts, and metric summaries.

It consumes cleaned records and questionnaire mappings, then emits auditable tables for reporting.
