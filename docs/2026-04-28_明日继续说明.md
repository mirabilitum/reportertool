# 2026-04-28 明日继续说明

## 当前状态

- Word 输入目录已改为 `F:\text file\datareport\data`。
- 该目录顶层有 43 个 Excel；每个同名子目录下有 1 个 DOCX，共发现 43 份 Word 问卷。
- `F:\reportertool` 已新增最小 Python 包结构：
  - `pyproject.toml`
  - `src/reportertool/__init__.py`
  - `src/reportertool/cli.py`
  - `src/reportertool/review_package.py`
  - `src/reportertool/questionnaire_normalize.py`
  - `src/reportertool/questionnaire_mapping.py`
  - `src/questionnaire/`
- 已运行问卷归一阶段，输出目录：
  - `F:\reportertool\outputs\questionnaire_normalized`

## 已跑通结果

运行命令：

```powershell
$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new($false)
$env:PYTHONPATH='F:\reportertool\src'
py -m reportertool.cli normalize-questionnaires --questionnaire-dir 'F:\text file\datareport\data' --out 'F:\reportertool\outputs\questionnaire_normalized'
```

结果：

- `source_files`: 43
- `questions`: 1779
- `answer_components`: 8857
- `normalized_groups`: 336
- `near_match_candidates`: 78
- `warning_count`: 0

主要输出：

- `normalized_question_mapping_long.csv`
- `normalized_question_mapping_wide.csv`
- `normalized_answer_components_long.csv`
- `near_match_review_candidates.csv`
- `normalized_question_mapping.xlsx`
- `questions_extracted.json`
- `review/stage_status.json`
- `review/questionnaire_anomalies.csv`

## 已发现问题

1. UTF-8 约束必须严格执行。后续 PowerShell 命令都要先设置：

```powershell
$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new($false)
```

不要把中文文件名硬写进 Python 临时脚本；优先枚举路径对象。

2. 学生表 subject 规范化还有问题。当前输出里学生表 subject 类似：

- `语文学科`
- `数学学科`
- `体育与健康学科`

需要统一去掉尾部 `学科`，与 Excel 标题解析结果保持一致。

3. 最后一条正在修改 `questionnaire_normalize.py` 的 subject 规范化补丁时被中断，明天第一步要先检查该补丁是否已经落盘，再决定是否补。

4. 新 Word 目录里“语文 54 题”定位到：

- role: `教研组`
- subject: `语文`
- q_no: `54`
- norm_id: `QG0038`
- question_type: `多选题`
- question_text: `本学科正在组织教师开展以下哪些方面的校本资源建设`

这与昨天旧目录中提到的“语文 54 题上传题/位置问题”不是同一个上下文，需要明天按新目录重新确认具体异常。

5. 本机 `pytest` 未安装。后续要么先不跑 pytest，要么经用户确认后安装依赖。

## 明天第一步

1. 检查 `src/questionnaire/constants.py` 中 `SUBJECT_ALIASES` 和 `src/questionnaire/docx_parse.py` 中 `infer_subject_role()`，确认是否已加入：
   - `学科课程实施情况表（学生）`
   - subject 尾部 `学科` 去除逻辑
2. 修正后重新运行 `normalize-questionnaires`。
3. 抽查：
   - `normalized_question_mapping_long.csv` 中学生表 subject 是否已统一为 `语文`、`数学` 等。
   - `normalized_answer_components_long.csv` 是否保留单选/多选选项、矩阵行列、上传组件。
   - `review/questionnaire_anomalies.csv` 是否仍为空或只有可解释 warning。
4. 再讨论“新目录下是否还有类似语文 54 题位置错乱”的问题。

## 明天唤起话术

可以直接说：

```text
继续昨天 reportertool。先看 F:\reportertool\docs\2026-04-28_明日继续说明.md，按里面的明天第一步继续。注意 UTF-8 工程约束，Word 输入目录现在是 F:\text file\datareport\data。先修正学生表 subject 里多出来的“学科”，重新跑 normalize-questionnaires，然后抽查答案组件和语文 54 题。
```
